---
import Layout from "../layouts/Layout.astro";

interface Props {
  title: string;
}

function heading(title: string): string {
  return decorate(title);
}

function decorate(title: string): string {
  return title.toUpperCase();
}
---

<Layout>
  <h1>{heading("home")}</h1>
</Layout>
